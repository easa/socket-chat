const server = require('./server')

test('should get messages', () => {
  expect(3).toBe(3)
})

test('should tell when/if a message is sent or not', () => {
  expect(3).toBe(3)
})

test('should tell when/if a message is delivered or not', () => {
  expect(3).toBe(3)
})

test('should tell when a friend comes online/goes offline', () => {
  expect(3).toBe(3)
})

test('should tell when a friend is typing', () => {
  expect(3).toBe(3)
})